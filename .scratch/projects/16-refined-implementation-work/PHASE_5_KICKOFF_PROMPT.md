# Phase 5 kickoff prompt

> Paste the block below into a fresh session to start Phase 5. It assumes Phases
> 0–4 are landed and pushed on `spine-refactor-phase-1` (HEAD `7624dcb`).

---

```
Please implement **Phase 5 of the TyO3 spine refactor — "One native `commit()` with staging and rollback"** — fully and correctly, following the step-by-step guide.

**PRIMARY GUIDE (read first, in full):**
@.scratch/projects/15-implementation-plan/PHASE_5_IMPLEMENTATION_GUIDE.md

**SUPPORTING CONTEXT (read the relevant sections as needed):**
- @.scratch/projects/15-implementation-plan/REFINED_IMPLEMENTATION_CONCEPT.md (§5.3 the single-writer commit transaction / "publish last" / "no torn publish", §5.10 the sidecar as a commit participant, §5.12 the typed error model / "do not swallow", §0.4 the test-only fault seam)
- @.scratch/projects/15-implementation-plan/REFINED_IMPLEMENTATION_PLAN.md (Phase 5 section + working rules + acceptance + commit series)
- @.scratch/projects/16-refined-implementation-work/PROGRESS.md (progress tracker — §8 is Phase 5; **§7 + the Phase-4 design-decision callout document exactly what Phase 4 delivered vs deferred** — read them. §17 is the live xfail countdown. UPDATE the tracker + dashboard (§2) + xfail countdown (§17) + defect #2 status (§1) when the phase is complete.)

**WHAT PHASE 5 IS (one line):** restructure the *commit transaction itself* so every write — `edit`, `edit_many`, `edit_virtual`, `sync_path`, `discard`, `sync_all`, `author`, watcher `poll_changes` — funnels through one native `commit(mutation)` that **stages** all next-state, does every fallible step (serialise, `write_atomic`, engine apply, code-layer update, delta compute) against the stage, and **publishes the revision LAST** — so a failure in any in-lock step rolls the whole commit back to R−1 with **no torn publish**, **no swallowed sidecar error**, and **no bus delta enqueued**, surfaced through typed exceptions.

**THIS PHASE CLOSES (or half-closes) THESE DEFECTS:**
- **Defect #2** (transaction split across the lock boundary) — Phase 5 fixes the *in-lock* half: stage→publish-last→rollback under the native `Mutex`. The bus/post-commit-path half is **Phase 6**. Mark #2 as "Phase 5 partial / Phase 6 completes" — do NOT close it outright.
- **Two Phase 1 carry-overs are explicitly IN SCOPE for Phase 5** (they belong to the commit funnel, not the content gate). Each has a strict-xfail that flips to a real failure the moment you fix it — that is your acceptance signal, remove the marker only once green:
  1. **`sync_all` must re-ingest disk, not just republish + Rescan.** Since the generation is now authoritative (Phase 1), today's `sync_all` (`head.system.publish(head.store.capture())` + `apply_changes(&[Rescan])`) republishes the existing generation and misses files created after open ("File not in project"). Fix: route `sync_all` through `ContentStore::ingest_project` (re-read the whole project as one batch) so new/changed/deleted files are picked up, then commit normally. Removes the xfail on `test_graph_build.py::TestGraphConstruction::test_content_hash_updates_incrementally_by_semantic_body`. *(Note: PROGRESS §17 currently shows this marker already removed by a Phase 4 test migration — verify the live state at the red baseline; the underlying `sync_all` fix is still required regardless.)*
  2. **The watcher's `apply_watch_events` must distinguish an unsaved buffer from ingested disk content.** It drops any event whose path returns `has_overlay(path) == true`. Phase 1 now interns *every* project file at open, so `has_overlay` is true for all of them and **every watcher event is dropped** (`poll_changes` returns `None`). Fix: track which paths carry a genuinely *unsaved overlay edit* (from `edit`/`edit_virtual`) separately from paths ingested from disk, and gate the buffer-wins rule on the former only. Removes xfails on `test_watch.py::{test_deleted_event, test_injected_change_matches_expected_delta, test_real_watcher_observes_disk_change}` (the last is strict=False — FS timing) and `test_gate8_bus.py::TestWatcherBus::test_inject_changes_fires_bus`. (Also flips the non-strict `graph/tests/test_incremental_parity.py::test_moved_entity_...`, which is outside the gated `testpaths` but should go green too.)

**⚠️ CRITICAL HANDOFF FROM PHASE 4 — the native code layer is EMPTY and the in-commit producer is DEFERRED (read PROGRESS §7 "Design decision (user-confirmed)"):**
Phase 4 (user-approved) shipped `CommitDeltaDto.code_delta` as a true `Option<CodeDeltaDto>` emitting **`None`** because the in-commit producer is **deferred** — so `head.code_layer` is empty, `reverse_deps` is empty, and `affected_ids` is seeds-only. Implications for Phase 5:
- Phase 5 stages the "code-layer update" step like every other staged step, but **do NOT** un-defer or run the producer here — that is a separate perf pass. The native commit's code-layer staging is effectively a near-no-op today (it emits `None`); the *post-commit head-graph rebuild* still happens in Python via `_apply_graph_delta`'s `None`→`full_code_delta()` path (Phase 4). Keep that consumer contract untouched.
- **The fault seam still needs a real `code_layer` stage boundary.** `test_final_transaction_rollback.py::test_code_layer_failure_publishes_no_partial_revision` arms the `"code_layer"` stage and asserts head does not advance. So `commit()` must have an explicit code-layer staging point — *before* publish — where `check_fault(head, "code_layer")?` fires `CommitFailed`, even though the producer that would populate it is deferred. Wire the seam at that boundary; do not skip the stage just because it currently stages nothing.

**Steps to implement IN THIS ORDER (deliberate; all detailed in the guide §4):**
- **5.4 (do FIRST)** — Typed errors. Add `SidecarWriteError`, `CommitFailed`, `ReconcileAmbiguous` to `rust/src/lib.rs` (each a subclass of `TyO3Error` via `create_exception!`, registered in module init), import into `project.rs`, and re-export from `src/tyo3/exceptions.py` (native import block + ImportError fallback stubs + `__all__`). Semantics: `SidecarWriteError` = a `write_atomic`/temp-rename failure persisting identity or an authored record (carries path + IO error); `CommitFailed` = any other in-lock failure that forced rollback; `ReconcileAmbiguous` = register + leave a `// raised once structural reconciliation can be ambiguous` note (no call site yet unless reconciliation already detects ambiguity). The rollback test catches `tyo3.exceptions.TyO3Error`, so subclassing matters.
- **5.2** — Stage all state, publish last, roll back on any failure (THE CORE). Capture the rollback baseline at the top of the commit body, build a `CommitStage` holding next-state, run all fallible work against it, and make `head.system.publish(...)` the **single, last** in-lock step. **Use Strategy A (clone-and-swap)** unless borrow constraints make it impossible (then Strategy B mutate-then-restore with a per-stage restore test) — write down which you chose. Concretely: remove the early `head.system.publish(head.store.capture())` (`project.rs:1444` and analogues in `sync_path_inner` `:1497`/`:1555`, `sync_all` `:2037`, `author` `:1958`) and move it to the tail; move identity persistence OUT of `run_identity_reconciliation` (`project.rs:1373`, today swallows the error at `:1410-1418`) so the commit does the staged `write_atomic` and **propagates** `SidecarWriteError` via `?` before publish.
- **5.1** — A single `commit(mutation) -> Result<CommitDeltaDto, PyErr>` funnel. Define a `Mutation` enum (`Overlay`/`SyncPath`/`SyncAll`/`Author`/`Poll`); the existing per-kind helpers become the match-arm bodies sharing the one staging+publish tail. PyO3 methods become thin (parse args → build `Mutation` → `commit` → pythonize/convert error). `poll_changes` routes through too; an empty event set still returns `None`/no-op — model "no events" as `commit` not publishing, NOT as a published empty revision.
- **5.3** — Fix authored-write ordering to **stage → persist → publish** on the same staging machinery. Today `author` (`project.rs:1927`) bumps+publishes at `:1958` then persists at `:1989` and rolls back only its in-memory map (`:1994`). Re-express as `Mutation::Author`: validate early (ConfigError/ValueError pre-stage), stage the COW store + `(record_path, bytes)`, `write_atomic` as staged step 6 (→`SidecarWriteError` on failure), publish last. **Delete** the in-memory-only `head.authored = prior` rollback (`:1983`/`:1994`) — with stage-then-publish there is no published state to restore.
- **5.5 (LAST)** — The native test-only fault-injection seam. Add `_fault_inject(stage: str)` on the PyO3 project (the first name the test probes; it also tries `fault_inject`/`_inject_commit_fault`). One-shot armed-fault cell consumed by the next commit; **inert when unarmed**; clearly documented as test-only (consider a `fault-injection` cargo feature so it can be stripped from release). Fire `check_fault(head, stage)?` at the real staged boundary for each stage — `"code_layer"` after code-layer staging, `"identity_persist"` at the identity `write_atomic`, `"authored_persist"` at the authored `write_atomic` — all **before** the single publish, take-and-clear so a stale arm can't leak. Stage names match the test's `_FAULT_STAGES`. Then remove the module-level `@pytest.mark.xfail` from `test_final_transaction_rollback.py` (`:62-66`) ONLY once all three cases pass on their own.

**WORKING RULES (non-negotiable):**
1. **Publication is the LAST in-lock step.** Exactly ONE `head.system.publish(...)` per commit, after every fallible step. Grep for `system.publish` and confirm no write path publishes before its persistence. This is the cardinal Phase 5 sin if violated.
2. **A failed commit advances NOTHING** — head, retained generations, identity registry, authored store, and sidecar all stay at R−1, and **no bus delta enqueued**. The rollback test asserts `s.head == before_head`, `s.id_for(...) == before_id`, `s.authored(...).status == "absent"`, and `sub.poll(timeout=0.0) is None` — honour every one.
3. **Never swallow a commit error.** Replace the identity-persist `log::error!(...)`-and-continue (`project.rs:1414`) with a propagated typed error. A deliberate non-failure (optional cache allowed absent) only with a one-line "why" comment; a sidecar write failure is NEVER tolerated.
4. **No Python-side write lock** — the native `Mutex` is the one writer boundary once publish is the tail. Do not add one.
5. **Don't reshape the delta or touch the bus.** Phase 5 changes *when/how* a commit becomes durable+published; it does NOT change the `CommitDelta` shape (Phase 3) or unify the Python post-commit path / bus (Phase 6). The success return value is the same `CommitDelta` callers already get.
6. **Write/await the failing test first.** `test_final_transaction_rollback.py` is strict-xfail today (no fault seam exists). Establish the red baseline (guide §3) before changing anything: confirm the rollback test fails in exactly the expected way, and that `test_write_path.py` + `test_gate6_authored.py` are green (your behaviour-preserving regression target). Never weaken a test.
7. Commits: NO AI attribution / no `Co-Authored-By` footers (project policy). Use the guide's §7 5-commit sequence (5.4 → 5.2 → 5.1 → 5.3 → 5.5); squash on landing if preferred (the plan's single commit is `refactor(commit): single native commit() with staging + rollback`).

**WATCH THE DOCUMENTED FAILURE MODES (guide §6):** forgetting `devenv shell -- build` before the rollback pytest (the test then probes the OLD extension and `_arm_fault` keeps calling `pytest.fail` "no matter what you fix"); publishing before persisting; swallowing the identity-persist error; rolling back only the in-memory authored map; Strategy-B restore gaps (a missed field is a silent partial rollback success-path tests won't catch — prefer Strategy A); a fault that fires AFTER publish (proves nothing); a leaky one-shot fault (clear it even when its stage isn't reached this commit); an empty `poll_changes` publishing a no-op revision; adding a Python write lock; reshaping the delta/bus; "optimising" the clone by sharing mutable state back into `head` before the publish tail (reopens the torn-publish window).

**DEV ENVIRONMENT — every in-repo command goes through devenv scripts:**
- Build the extension: `devenv shell -- build` (**REQUIRED after every Rust change before pytest** — Phase 5 adds native exceptions + a native fault seam that the Python rollback test exercises through the session; a green `cargo test` does NOT refresh the compiled extension Python imports).
- Fast Python tests (parallel, no-cov; accepts narrowed targets): `devenv shell -- test-fast test_final_transaction_rollback.py`
- Rust tests: `devenv shell -- cargo test --manifest-path rust/Cargo.toml <one-filter-at-a-time>` (e.g. `project authored sidecar identity`)
- Suites take ~10–15 min; run full suites in the background and wait (background tasks, not foreground sleeps).

**CURRENT REPO STATE you're starting from (branch `spine-refactor-phase-1`, HEAD `7624dcb`, pushed):**
- Phases 0,1,2,3,4 are DONE, committed, and pushed. Phase 4 = commits `41b838a` (4.1 applier) · `13f2b08` (4.2a head/post-commit native + `Option<CodeDeltaDto>` three-state) · `9bba822` (4.4 snapshot frozen-db delta) · `35d3e47` (4.2b+4.3 delete read-surface build + priming); `7624dcb` is the Phase 4 docs/tracker update.
- Milestone gate green EXCEPT 3 pre-existing later-phase failures and the xfail-tracked tests. **Rust: 156 passed / 0 failed. Python: 647 passed, 16 xfailed, 3 failed** — the 3 = documented baseline (`test_final_derived_contract` ×2 → Phase 7; `test_final_hash_ast::test_formatting_only_hashes_same` → Phase 9). Do NOT add NEW failures.
- **xfails Phase 5 must close (PROGRESS §17):** `test_final_transaction_rollback.py` (3, module-level) + `test_watch.py` (3) + `test_gate8_bus.py::test_inject_changes_fires_bus` (1). The remaining open xfails (`test_final_bus_contract` ×3 → Phase 6; `test_final_derived_contract` ×5 → Phase 7; `test_final_hash_ast` ×2 → Phase 9) are NOT yours to touch.
- The sidecar write primitive is already crash-safe — `Sidecar::write_atomic` (`rust/src/sidecar.rs:76`) writes `.tmp` + fsync + atomic rename. Phase 5 does NOT reinvent it; it routes every commit's sidecar writes through the staging boundary and stops swallowing the error.

**ACCEPTANCE (guide §5):** the focused gate — `test_final_transaction_rollback.py` (all 3 cases green, module xfail removed) + `test_write_path.py` + `test_gate6_authored.py` green, plus `cargo test project authored sidecar` — AND the two carry-overs' xfails removed (`test_watch.py` ×3, `test_gate8_bus::test_inject_changes_fires_bus`). Then the full milestone gate: `devenv shell -- pytest -q --no-cov` and `devenv shell -- cargo test --manifest-path rust/Cargo.toml`, both green at the documented baseline with zero new failures. Exit criteria (all must hold): every write funnels through one `commit(mutation)`; exactly one publish per commit, last; a failed write advances nothing and enqueues no bus delta; no sidecar error swallowed; authored write is stage→persist→publish with the in-memory `prior` rollback deleted; the three typed errors exist and are re-exported from `tyo3.exceptions`; the fault seam is test-only, one-shot, inert when unarmed. Update PROGRESS.md §8 + dashboard (§2) + xfail countdown (§17) + defect #2 status (§1, "Phase 5 partial") when complete.

Start by reading the Phase 5 guide and establishing the red baseline (guide §3), then work the steps in the order above. **Ask me before making any design decision the guide leaves open** — in particular: (a) Strategy A clone-and-swap vs Strategy B mutate-then-restore if borrow constraints bite (guide recommends A; confirm before going B), and (b) whether to gate the fault seam behind a `fault-injection` cargo feature vs an always-compiled-but-inert field.
```
